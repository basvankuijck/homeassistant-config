---
# The notify integration makes it possible to send notifications to a wide variety of platforms
#
# https://www.home-assistant.io/components/notify/
#
notify:
  - name: all
    platform: group
    services:
      - service: ios_iphone_x_bas
      - service: mobile_app_iphone_x_bas
